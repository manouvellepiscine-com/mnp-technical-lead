{
  "scripts": {
    "start": "docker compose up",
    "edgedb:migration-create": "docker exec -it edgedb edgedb --tls-security=insecure -H localhost migration create --non-interactive",
    "edgedb:migrate": "docker exec -it edgedb edgedb --tls-security=insecure -H localhost migrate",
    "edgedb:edgeql-local": "docker exec -it elysia bunx @edgedb/generate edgeql-js --dsn edgedb://edgedb:edgedb@edgedb:5656?tls_security=insecure --target=ts --output-dir=./dbschema"
  }
}
